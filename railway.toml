[railway]
language = "python"
# Build from repo root — Dockerfile at root copies backend/

[build]
builder = "dockerfile"

[deploy]
startCommand = "python -m uvicorn main:app --host 0.0.0.0 --port 8000"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10