fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0
# asyncpg excluded — Railway uses Python 3.13 which has no prebuilt wheels for asyncpg==0.29.0
# The app runs on SQLite (aiosqlite) so asyncpg is not strictly needed
# asyncpg==0.29.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
python-dotenv==1.0.1
resend==2.0.0
stripe==8.11.0
apscheduler==3.10.4
pydantic[email]==2.9.0